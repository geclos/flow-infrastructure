---
- name: Create deployment user
  become: true
  user: name=deploy comment="Application deployment user" shell="/bin/bash"

- authorized_key: user=deploy key=https://github.com/{{item}}.keys
  with_items:
    - geclos
  become: true

- set_fact:
    deploy_user_created: yes
